<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="com.template.mvvm">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:name=".App"
        android:allowBackup="false"
        tools:replace="allowBackup"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.template.mvvm.ExAppGlideModule"
            android:value="ExAppGlideModule"/>

        <activity
            android:name=".splash.SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".home.HomeActivity"
            android:theme="@style/HomeTheme"/>


        <activity
            android:name=".products.ProductsActivity"
            android:theme="@style/ProductsTheme"/>


        <activity
            android:name=".products.detail.ProductDetailActivity"
            android:theme="@style/ProductDetailTheme"/>

        <activity
            android:name=".about.AboutActivity"
            android:theme="@style/AboutTheme"/>


        <activity
            android:name=".licenses.SoftwareLicensesActivity"
            android:theme="@style/SoftwareLicensesTheme"/>
    </application>
</manifest>
