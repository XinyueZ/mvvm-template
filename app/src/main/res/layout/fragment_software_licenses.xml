<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
    >

    <data>

        <import type="com.template.mvvm.binding.recycler.LayoutManagerTypeKt"/>

        <variable
            name="vm"
            type="com.template.mvvm.models.SoftwareLicensesViewModel"/>
    </data>

    <android.support.design.widget.CoordinatorLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:goBack="@{vm.goBack}">


        <android.support.design.widget.AppBarLayout
            android:id="@+id/appbar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"
            app:elevation="15dp">


            <android.support.v7.widget.Toolbar
                android:id="@+id/toolbar"
                android:layout_width="match_parent"
                android:layout_height="?attr/actionBarSize"
                android:background="?attr/colorPrimary"
                android:minHeight="?attr/actionBarSize"
                android:theme="@style/Toolbar"
                app:layout_scrollFlags="scroll|enterAlways|snap"
                app:navigationIcon="@drawable/ic_arrow_back"
                app:onIndicatorClick="@{ vm::toggleBack }"
                app:popupTheme="@style/ThemeOverlay.AppCompat.Light"
                app:title="@{vm.title}"/>

        </android.support.design.widget.AppBarLayout>


        <android.support.v7.widget.RecyclerView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:binding="@{vm.listBinding}"
            app:factory="@{vm.listFactory}"
            app:items="@{vm.libraryList}"
            app:layoutManager="@{LayoutManagerTypeKt.LINEAR_VERTICAL}"
            app:layout_behavior="@string/appbar_scrolling_view_behavior"
            />

        <include
            layout="@layout/layout_collection_progress_bar"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_gravity="center"
            app:dataLoaded="@{vm.dataLoaded}"
            app:loadingText="@{vm.loadingText}"/>


    </android.support.design.widget.CoordinatorLayout>
</layout>