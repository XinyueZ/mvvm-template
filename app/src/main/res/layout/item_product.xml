<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools">

    <data class="ProductItemBinding">

        <import type="com.template.mvvm.R"/>
        <variable
            name="vm"
            type="com.template.mvvm.models.ProductItemViewModel"/>
    </data>

    <android.support.v7.widget.CardView
        style="@style/RecyclerViewCardView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:commandItem="@{vm::onCommand}"
        app:vm="@{vm}">


        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <FrameLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content">

                <ImageView
                    android:id="@+id/thumnailIv"
                    android:layout_width="match_parent"
                    android:layout_height="@dimen/item_thumbnail_size"
                    android:contentDescription="@null"
                    app:errorDrawableRes="@{R.drawable.ic_image}"
                    app:placeholderRes="@{R.drawable.ic_image}"
                    app:remoteImageUri="@{vm.thumbnail}" />


                <ImageView
                    android:id="@+id/brandLogoIv"
                    android:layout_width="@dimen/item_brand_logo_size"
                    android:layout_height="@dimen/item_brand_logo_size"
                    android:layout_gravity="top|end|right"
                    android:contentDescription="@null"
                    app:errorDrawableRes="@{R.drawable.ic_image}"
                    app:placeholderRes="@{R.drawable.ic_image}"
                    app:remoteImageUri="@{vm.brandLogo}" />
            </FrameLayout>

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginEnd="5dp"
                android:layout_marginLeft="5dp"
                android:layout_marginRight="5dp"
                android:layout_marginStart="5dp"
                android:gravity="center_vertical|start|end"
                android:inputType="textNoSuggestions|textMultiLine"
                android:text="@{vm.title}"
                android:textAppearance="@style/MaterialTextView.LightTheme.Title"
                android:textColor="@color/colorBlack"
                tools:text="title"/>


            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginEnd="5dp"
                android:layout_marginLeft="5dp"
                android:layout_marginRight="5dp"
                android:layout_marginStart="5dp"
                android:gravity="center_vertical|start|end"
                android:inputType="textNoSuggestions"
                android:text="@{vm.description}"
                android:textAppearance="@style/MaterialTextView.LightTheme.Caption"
                android:textColor="@color/colorBlack"
                tools:text="description"/>
        </LinearLayout>
    </android.support.v7.widget.CardView>
</layout>
