<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tool="http://schemas.android.com/tools">

    <android.support.constraint.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/naviHeaderBackground">

        <ImageView
            android:id="@+id/naviThumbIv"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="@dimen/material_margin_content"
            android:layout_marginStart="@dimen/material_margin_content"
            android:background="@color/colorTransparent"
            android:src="@mipmap/ic_launcher_round"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tool:src="@mipmap/ic_launcher_round"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginLeft="15dp"
            android:layout_marginStart="15dp"
            android:background="@color/colorTransparent"
            android:text="@string/app_name"
            android:textAppearance="@style/MaterialTextView.LightTheme.Title.Medium"
            android:textColor="@color/colorWhite"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintLeft_toRightOf="@+id/naviThumbIv"
            app:layout_constraintStart_toEndOf="@+id/naviThumbIv"
            app:layout_constraintTop_toTopOf="parent"
            tool:text="@string/app_name"/>

    </android.support.constraint.ConstraintLayout>
</layout>